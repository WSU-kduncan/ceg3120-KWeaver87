# Personalizations
# Configure frontend for port 80 and to use "servers" backend.
frontend http-in
        bind :80
        default_backend servers

# Configure backend as roundrobin with health checks
backend servers
        balance roundrobin
        server s1 httpd-1:80 check
        server s2 httpd-2:80 check